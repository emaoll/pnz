<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	package="com.acceleratedio.pac_n_zoom"
	android:versionCode="011"
	android:versionName="0.1.1" >

	<uses-sdk
        tools:node="merge"
        android:minSdkVersion="14"
        android:targetSdkVersion= "19" />

	<uses-permission android:name="android.permission.INTERNET" />		
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />

	<application
		android:allowBackup="true"
		android:icon="@drawable/io_icon"
		android:label="@string/app_name"
		android:theme="@style/AppTheme" >

		<activity
			android:name="com.acceleratedio.pac_n_zoom.PickAnmActivity"
			android:theme="@android:style/Theme.NoTitleBar"
			android:windowSoftInputMode="stateHidden" />


        <activity
			android:name="com.acceleratedio.pac_n_zoom.FindTagsActivity"
			android:theme="@android:style/Theme.NoTitleBar"
			android:windowSoftInputMode="stateHidden"
		/>

		<activity
			android:name="com.acceleratedio.pac_n_zoom.SaveAnmActivity"
			android:theme="@android:style/Theme.NoTitleBar"
			android:windowSoftInputMode="stateHidden"
			android:noHistory="true"
		/>

		<activity
			android:name="com.acceleratedio.pac_n_zoom.SelectImageActivity"
			android:label="@string/app_name" >
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

    <activity android:name="com.acceleratedio.pac_n_zoom.AnimActivity"></activity>
    <activity android:name="com.acceleratedio.pac_n_zoom.ViewVideos"></activity>
	</application>
</manifest>
